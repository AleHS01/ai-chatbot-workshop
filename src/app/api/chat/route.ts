export async function POST(req: Request) {
//   get the user message from the request body

// call the chat api with the user message

// stream the response to the client

// catch any errors and return a 500 error
}